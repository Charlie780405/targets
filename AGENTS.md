# AGENTS.md — 靶点情报引擎 · 工程代理规则（仓库级持续指导）

本文件是 Codex / Cursor Agent 在本仓库工作的**固定工程规则**。每次开工先读本文件与 `docs/agent-context/workflow.md`。
可复用的数据接入 / 报告工作流后续封装为 Skill；本文件承载稳定的仓库级约束。

## 0. 一句话定位

系统的产品单位是**事件**（Event），不是文章。任何采集、抽取、报告都最终服务于「产出可追溯、可决策的事件与周报」。

## 1. 红线（违反任一条视为未完成）

1. **绝不提交密钥与运行时数据**：`.env*`、`*.pem/*.key`、`data/raw/**`、`data/logs/**`、`*.sqlite`、Vault 内容一律不入库。
2. **每条事件必须可追溯**：无原始 URL + 来源名称 + 证据片段 + 内容哈希的「事件」不得写入库、不得进周报。
3. **无人工审核不出医学结论**：`medical_review_status != approved` 的事件不得作为周报的「关键结论」发布（可列为「待核」）。
4. **证据分离评分**：`importance_score` / `confidence_score` / `novelty_score` 必须分列存储，禁止合并成单一分数。
5. **精确提交**：`git add <路径>`，禁止 `git add .`；仅在用户明确要求时 commit/push。
6. **PLAN / verify / 代码同批入库**：文档与实现一起提交，不得事后补。

## 2. 工程约定

- 语言 Python 3.12+。类型用 Pydantic v2 建模；对外/落库边界必须校验，内部信任。
- 采集优先级：**官方 API > RSS/结构化页面 > 稳定网页抓取 > 搜索引擎发现**。能用 API 绝不写爬虫。
- 网页自动化（Playwright）仅在无 API/RSS 时启用，且封装进对应「适配器」，不做通用爬虫。
- 每个数据源 = 一个 `packages/source_adapters/<source>/` 适配器，实现统一接口：`fetch() -> list[SourceDocument]`。
- **禁止硬编码**靶点名、来源名、事件类型、评分权重等口径值——一律引用 `config/` 下 SSOT 或 `packages/domain` 枚举。
- 抓取必须礼貌：遵守 `HTTP_MAX_RPS`、带 `User-Agent`、尊重 robots 与各源许可；付费墙全文不抓。

## 3. 测试与验证命令

```bash
# 单测（真实 HTTP 用 VCR.py 录制回放，禁止测试打真实外网）
pytest -q

# 静态检查
ruff check .
mypy .

# PLAN 专项验证（每个 PLAN 一支）
bash scripts/verify-plan-XXX.sh

# PLAN 目录布局校验（提交前）
bash scripts/validate-plan-dir-layout.sh
```

新增数据源适配器**必须**附带 VCR cassette 与去重/字段差异测试。

## 4. 情报引用与证据分级（强制）

所有情报分析、事件笔记、竞品评估等输出，必须遵循以下来源和引用规则。**违反本条视为未完成。**

### 4.1 来源精度三级制

每一条关键数据必须标注来源精度等级，写在数据引用处：

| 等级 | 含义 | 例 |
|:----|:-----|:----|
| **A** | **一手官方来源** — 公司新闻稿、ClinicalTrials.gov 注册信息、FDA/EMA/CDE 公示、同行评审期刊（NEJM/Lancet/JAMA）、正式大会 LBR 摘要 | Aclaris 新闻稿 2026.4.28; NCT06102005; NEJM 2016, Simpson et al. |
| **B** | **同行评审二级文献** — PMC/PubMed 综述、系统评价、荟萃分析，非原始研究但经同行评议 | PMC11196376; Front Immunol 2026 DOI:10.3389/... |
| **C** | **行业报道 / 媒体引用** — 需经交叉验证或显式标注"行业报道声称……，公司官方未确认" | pharmacally.com; Practical Dermatology 2025.10; Fierce Biotech |

### 4.2 引用格式规范

每条数据的引用必须包含：**来源类型 + 日期 + 可访问的标识符**

```
✅ 正确写法：
  赛诺菲新闻稿 2026-03-28, sanofi.com; NCT06130566 (A级)
  → 读者知道是一手来源（A级），知道精确日期和内容

❌ 错误写法（模糊，不可追溯）：
  Pfizer 新闻稿 2025 （哪个？哪条数据？）
  ATS 2026 （大会还是摘要？没链接）
  临床研究显示 （哪家？什么研究？）
```

### 4.3 强制标注场景

以下场景**必须**标注来源等级，否则视为数据无法追溯：

- **疗效数据**（EASI-75、vIGA 0/1、p 值等）— 必须标注 A 或 B
- **临床试验结果定性**（"阳性" / "阴性" / "未达主要终点"）— 必须标注来源
- **安全性信号**（SAE、死亡案例、特殊不良事件）— 必须标注 A
- **财务数据**（交易对价、收入预测）— 必须标注 A
- **管线进度**（阶段、NCT ID）— 必须标注 A（ClinicalTrials.gov）

### 4.4 精确认知边界

当数据是从行业报道间接获取（非一手来源），必须说明：

```
Tilrekimig EASI-75 高剂量 49.4%
→ 来源：pharmacally.com 引用 NCT05995964 (C级)
→ 说明：Pfizer 官方新闻稿（A级）称"两个最高剂量水平可能优于标准治疗"，
  但未公开精确 EASI-75 数值。该精确值来自行业报道，需 Pfizer 后续正式公布确认。
```

### 4.5 医学证据要求（原文保留）

- 结论级输出必须给出证据链：`ClinicalTrials.gov / PubMed / 公司稿 / 大会摘要` 至少一条一手来源（A级）。
- 「记录更新」≠「医学意义进展」：字段 diff 必须翻译成事件类型后才计入情报。
- 中文输出，**保留英文原始证据片段**（标题、终点、数值原文）。
- 不确定必须显式标注（`confidence_score` + 文字「待核实/单一来源」）。

## 5. 目录职责

| 目录 | 职责 |
| --- | --- |
| `apps/collector` | 调度与运行各源适配器，落原始 `SourceDocument` |
| `apps/processor` | 清洗、实体解析、去重、版本 diff、生成 `Event`、评分 |
| `apps/reporter` | 日报/周报生成、Obsidian Markdown 导出、邮件/网页 |
| `packages/domain` | 领域模型与枚举（Event/Target/Asset/... 的 SSOT） |
| `packages/source_adapters` | 各数据源适配器 |
| `packages/entity_resolution` | 靶点词典、药物解析、公司归一 |
| `packages/obsidian_exporter` | Vault frontmatter 与目录写入 |
| `config/` | 靶点别名、适应症、来源白名单/证据等级（YAML SSOT） |
| `prompts/` | LLM 抽取/摘要提示词（版本化） |
| `docs/` | 规格、agent-context、plans、walkthroughs |

## 6. 交付流程（简版，完整见 `docs/agent-context/workflow.md`）

```
需求澄清 → PLAN(±子计划) → 用户确认 → feat 分支 → 实现 → verify → 精确 commit
    → merge main(--no-ff) → (用户要求才 push/部署) → WT 验收
```

- PLAN 全部放 `docs/plans/PLAN-XXX-<slug>/`（禁止根目录散落）。
- 每 PLAN 一支 `scripts/verify-plan-XXX.sh`，结束打印 `pass/total`。
- WT 放 `docs/walkthroughs/WT-XXX-<slug>.md`，逐条回填 PLAN 验证清单。

## 7. 思维准则：避免信息茧房

涉及情报分析、策略建议、方案评估等输出时（包括日报、周报、事件研判），须按照 `avoid-information-cocoons` skill 执行六步框架：

1. **区分目标与方法** — 用户的描述路径不一定是唯一或最优解
2. **隐藏假设审查** — 识别未经验证的前提、路径依赖、思维定式
3. **信息差扫描** — 补充用户未提及的技术/规则/范式/国外实践等，变化信息先核实
4. **替代路径** — 思考更简单、更稳定、上限更高、更长期、AI时代的新方案
5. **反方审查** — 失败点、20/80方案、激进方案、从零设计视角、高手未普及方法
6. **执行决策** — 明确推荐路线及理由，不堆选项
7. **对抗性复核** — 交付前自查关键遗漏、逻辑跳跃、隐性成本，发现问题直接修正

## 8. 微信文章 / 外部链接 分析处理流程（强制）

用户发来的微信文章链接、行业新闻链接等，按以下流程处理：

### 8.1 处理六步

1. **提取全文** — 读取文章内容
2. **来源甄别** — 标注入口来源（如"bioSeedin柏思荟 公众号, 2026-07-27"，属于 C 级），区分文章本身与文章内引用的一手数据
3. **结构化** — 按标准格式写入：事件概要 → 核心数据表（逐条标注来源等级 A/B/C）→ 底层原因分析 → 行业启示
4. **交叉搜索验证** — 对文章中的关键声明（疗效数据、安全性信号、交易金额等）独立搜索验证，验证方法与第 4 节一致
5. **关联** — 在笔记中显式关联知识库已有相关笔记（"参见 Amlitelimab 分析"、"TSLP/IL-13 靶点全景"等），更新相关笔记的关联字段
6. **落库** — 按 vault 规则写入 01-Targets/ 或 07-Events/，含完整来源表

### 8.2 来源精度约束

- 微信公众号文章本身 → 属于 **C 级**来源
- 文章内引用的外部数据（如"赛诺菲新闻稿称……"）→ 必须溯源到一手来源，标注该一手来源的 A/B/C 等级
- 原始来源不可访问（付费墙、失效链接）→ 显式标注「原始来源不可直接验证」
- 文章中的图表数据 → 尽量找到原始出处；找不到时标注「引自原文，原始出处未公开」

### 8.3 交叉验证结论标注

| 验证结果 | 标注方式 |
|:---------|:---------|
| 与一手来源一致 | 标注「✅ 已验证」+ 附一手来源引用 |
| 与一手来源有出入 | 标注「⚠️ 与一手来源不一致：……」，以一手来源为准 |
| 无法验证（无公开数据） | 标注「⏳ 待验证：该声明未经独立来源确认」|

### 8.4 关联索引维护

每条入库的笔记，在 `related_notes` 字段中列出关联的既有笔记路径。同时反向更新已有笔记的 `related_notes` 字段，确保双向索引可用。
